# Claude Bridge Environment Configuration Template
# Copy this file to ~/.claude-bridge/.env and configure your settings

# Port for the FastAPI server
PORT=8008

# Host for the FastAPI server (usually 127.0.0.1 for localhost)
HOST=127.0.0.1

# tmux session name
TMUX_SESSION=claude

# tmux session and pane target (session:window.pane)
TMUX_TARGET=claude:0.0

# Maximum number of lines to capture from tmux
MAX_CAPTURE_LINES=2000

# Delay in milliseconds after sending command before capturing output
CAPTURE_DELAY_MS=1500

# Base directory for Claude Bridge data (default: $HOME/.claude-bridge)
BRIDGE_BASE_DIR=$HOME/.claude-bridge

# Path to the bearer token file
BEARER_TOKEN_FILE=$HOME/.claude-bridge/token.txt

# Path to the log file
LOG_FILE=$HOME/.claude-bridge/logs/bridge.log

# Path to the jobs/state directory
STATE_DIR=$HOME/.claude-bridge/jobs

# Path to the logs directory
LOG_DIR=$HOME/.claude-bridge/logs

# Python virtual environment path
VENV_PATH=$HOME/.venvs/claude-bridge

# Binary directory for scripts
BIN_DIR=$HOME/bin

# LaunchAgent identifier (change this to your own)
LAUNCH_AGENT_ID=com.user.claude-bridge
