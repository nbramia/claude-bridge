# Claude Bridge Environment Configuration Template
# Copy this file to ~/.claude-bridge/.env and configure your settings

# Port for the FastAPI server
PORT=8008

# tmux session and pane target
TMUX_TARGET=claude:0.0

# Maximum number of lines to capture from tmux
MAX_CAPTURE_LINES=2000

# Delay in milliseconds after sending command before capturing output
CAPTURE_DELAY_MS=1500

# Path to the bearer token file
BEARER_TOKEN_FILE=$HOME/.claude-bridge/token.txt

# Path to the log file
LOG_FILE=$HOME/.claude-bridge/logs/bridge.log
